import { fetchData } from '../service/fetchService'

export const picture = (state={pictures: [], loading: false}, action) => {
  switch(action.type){
    case 'FETCHING':
      return
    case 'FETCHED':
      return
  }
}
